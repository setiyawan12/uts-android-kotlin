<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.setiyawan.uts_android_kotlin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Utsandroidkotlin">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DetailActivity" />
        <activity android:name=".profile.SetiyawanActivity" />
        <activity android:name=".KalkulatorActivity" />
        <activity android:name=".LoginActivity">
        </activity>
        <activity android:name=".TentangActivity" />
        <activity android:name=".BeritaActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>